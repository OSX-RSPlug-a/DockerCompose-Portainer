# DockerCompose-Portainer

Using the Scrpit:

Create a folder in the projects folder and run the command.

  docker-compose up -d

This will pull and run the docker image to admin your another dockers on: 

  localhost:9000
